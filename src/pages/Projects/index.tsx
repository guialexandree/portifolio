import { Title } from '@/components'
import './styles.scss'

const Projects: React.FC = () => {
  return (
    <section className='resume-container' id='projetos'>
      <Title title='projetos' subtitle='projetos' />
    </section>
  )
}

export default Projects
